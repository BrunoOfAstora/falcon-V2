#include "falconinit.h"

int flcn_verify(const char *f_name)
{
	int ret_code = 1;
	bool name_found = false;
	bool hash_found = false;
	
	sqlite3_stmt = *stmt;

	flcn_save->full_db_path = ;

	const char *sql_request = "SELECT";
	

end_func:

	return ret_code;
}
